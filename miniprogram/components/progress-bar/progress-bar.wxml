<!--components/progress-bar/progress-bar.wxml - 进度条组件-->
<view class="progress-wrapper">
  <view class="progress-track">
    <view class="progress-fill" style="width: {{percent}}%"></view>
  </view>
  <text class="progress-label" wx:if="{{showLabel}}">{{percent}}%</text>
</view>